# MIGRATION_EXCEPTIONS.md — قالب تقرير الاستثناءات

> يُملأ تلقائياً من `reports/migration-summary.json` بعد الـ dry-run. هذا القالب يوضّح الأنواع المتوقّعة. **لا سجل يُهمَل صامتاً — كل استثناء هنا للمراجعة البشرية.**

## 1) دفعات بلا حركة خزنة (financialReview)
دفعات تاريخية لا يوجد لها `treasury_txn` مطابقة. تُرحّل بحالتها، `treasury_txn_id=null`. **لم تُخترع لها حركة.**

| paymentId | studentId | amount | date | ملاحظة |
|-----------|-----------|--------|------|--------|
| _(يُملأ)_ | | | | دفعة تاريخية بلا خزنة |

## 2) مبالغ matDist بلا خزنة/دفعة
`paidAmount` موجود في matDist بلا payment/treasury حقيقي. محفوظ كـ `legacy_metadata` فقط.

| matId | studentId | paidAmount | payStatus | ملاحظة |
|-------|-----------|-----------|-----------|--------|
| _(يُملأ)_ | | | | legacy_metadata فقط — NO INVENTED MONEY |

## 3) مرشّحو دمج أولياء الأمور (duplicateCandidates)
نفس الرقم المطبّع باسمين مختلفين — دُمجوا تحت parent واحد.

| phone (normalized) | الأسماء | ملاحظة |
|--------------------|---------|--------|
| _(يُملأ)_ | | مراجعة: هل نفس الشخص؟ |

## 4) حقول بلا mapping (unmappedFields)
حقول في المصدر بلا عمود مقابل. **لم تُفقد** — مُبلّغة هنا لتقرير ما إذا كانت مطلوبة.

| collection | الحقول |
|-----------|--------|
| _(يُملأ)_ | |

## 5) كلمات مرور نصّية
مستخدمون بكلمات مرور نصّية قديمة — تحتاج hashing قبل الإنتاج.

| userId | ملاحظة |
|--------|--------|
| _(يُملأ)_ | plaintext — hash لاحقاً |

## 6) سجلات متخطّاة (skipped)
سجلات بلا mapping معرّف أو ببنية غير متوقّعة.

| collection | السبب |
|-----------|-------|
| _(يُملأ)_ | |
